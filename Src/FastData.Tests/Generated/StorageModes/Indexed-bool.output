// <auto-generated />
#nullable enable
using Genbox.FastData;
using Genbox.FastData.Abstracts;
using Genbox.FastData.Helpers;
using System.Runtime.InteropServices;
using System.Runtime.CompilerServices;
using System.Text;
using System;


internal static class ImmutableSet 
{
    private static readonly int[] _buckets = { 2, 0 };

    private static readonly Entry[] _entries = { 
        new Entry(885181228, -1, true),
        new Entry(0, 0, false)
    };

    
    public static bool Contains(bool value)
    {


        uint hashCode = unchecked((uint)HashHelper.Hash(value, 0));
        uint index = hashCode & 1;
        int i = _buckets[index] - 1;

        while (i >= 0)
        {
            ref Entry entry = ref _entries[i];

            if (entry.HashCode == hashCode && entry.Value.Equals(value))
                return true;

            i = entry.Next;
        }

        return false;
    }

    [StructLayout(LayoutKind.Auto)]
    private struct Entry
    {
        public uint HashCode;
        public short Next;
        public bool Value;

        public Entry(uint hashCode, short next, bool value)
        {
            HashCode = hashCode;
            Next = next;
            Value = value;
        }
    }

    public const int ItemCount = 2;
}
