// <auto-generated />
#nullable enable
using Genbox.FastData;
using Genbox.FastData.Abstracts;
using Genbox.FastData.Helpers;
using System.Runtime.InteropServices;
using System.Runtime.CompilerServices;
using System.Text;
using System;


internal static class ImmutableSet 
{
    private static readonly int[] _buckets = { 2, 3, 0 };

    private static readonly Entry[] _entries = {
        new Entry(4294967295, -1, -1),
        new Entry(0, 0, 0),
        new Entry(1, -1, 1)
    };

    
    public static bool Contains(sbyte value)
    {
        if (value < -1 || value > 1)
           return false;

        uint hashCode = unchecked((uint)value);
        uint index = MathHelper.FastMod(hashCode, 3, 6148914691236517206);
        int i = _buckets[index] - 1;

        while (i >= 0)
        {
            ref Entry entry = ref _entries[i];

            if (entry.HashCode == hashCode && entry.Value.Equals(value))
                return true;

            i = entry.Next;
        }

        return false;
    }

    [StructLayout(LayoutKind.Auto)]
    private struct Entry
    {
        public uint HashCode;
        public short Next;
        public sbyte Value;

        public Entry(uint hashCode, short next, sbyte value)
        {
            HashCode = hashCode;
            Next = next;
            Value = value;
        }
    }

    public const int ItemCount = 3;
}
