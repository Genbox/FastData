// <auto-generated />
// This file is auto-generated. Do not edit manually.
// Structure: HashTable
#nullable enable
using System;
using System.Collections.Generic;
using System.Runtime.CompilerServices;
using System.Runtime.InteropServices;

internal static class FastData
{


    [StructLayout(LayoutKind.Auto)]
    private struct E
    {
        internal int Key;
        internal sbyte Next;
        internal E(int key, sbyte next)
        {
            Key = key;
            Next = next;
        }
    };

    private static readonly byte[] _buckets = new byte[] {
        3, 4, byte.MinValue, byte.MinValue, byte.MinValue, 2, byte.MinValue, 5, 1, byte.MinValue
    };

    private static readonly E[] _entries = {
        new E(int.MinValue, -1), new E(-1, -1), new E(0, -1), new E(1, -1), new E(int.MaxValue, -1)
    };

    [MethodImpl(MethodImplOptions.AggressiveInlining)]
    private static ulong Hash(int value)
    {
        unchecked
        {
        return (ulong)value;
        }
    }

    public static IEnumerable<int> Keys
    {
        get
        {
            for (int i = 0; i < _entries.Length; i++)
                yield return _entries[i].Key;
        }
    }

    
    public static bool Contains(int key)
    {


        ulong hash = Hash(key);
        int index = (int)(hash % 10);
        int i = (int)_buckets[index] - 1;

        while (i >= 0)
        {
            ref E entry = ref _entries[i];

            if (entry.Key == key)
                return true;

            i = entry.Next;
        }

        return false;
    }

    public const int ItemCount = 5;
    public const int MinKey = int.MinValue;
    public const int MaxKey = int.MaxValue;
}