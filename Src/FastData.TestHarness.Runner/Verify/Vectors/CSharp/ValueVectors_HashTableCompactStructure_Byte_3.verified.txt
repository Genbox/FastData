// <auto-generated />
// This file is auto-generated. Do not edit manually.
// Structure: HashTableCompact
#nullable enable
using System;
using System.Runtime.CompilerServices;
using System.Runtime.InteropServices;

internal static class HashTableCompactStructure_Byte_3
{
    [StructLayout(LayoutKind.Auto)]
    private struct E
    {
        internal byte Key;
        internal E(byte key)
        {
            Key = key;
        }
    };

    private static readonly byte[] _bucketStarts = new byte[] {
        0, 2, 3
    };

    private static readonly byte[] _bucketCounts = new byte[] {
        2, 1, 0
    };

    private static readonly E[] _entries = {
        new E(byte.MinValue), new E(byte.MaxValue), new E(1)
    };


    [MethodImpl(MethodImplOptions.AggressiveInlining)]
    private static ulong Hash(byte value)
    {
        return (ulong)value;
    }


    public static bool Contains(byte key)
    {
        if (key < byte.MinValue || key > byte.MaxValue)
            return false;


        ulong hash = Hash(key);
        uint index = (uint)(hash % 3);
        int start = (int)_bucketStarts[index];
        int count = (int)_bucketCounts[index];
        int end = start + count;

        for (int i = start; i < end; i++)
        {
            ref E entry = ref _entries[i];

            if (entry.Key == key)
                return true;
        }

        return false;
    }

    public const uint ItemCount = 3;
    public const byte MinKey = byte.MinValue;
    public const byte MaxKey = byte.MaxValue;
}