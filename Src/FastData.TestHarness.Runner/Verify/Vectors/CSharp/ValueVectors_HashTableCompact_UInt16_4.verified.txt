// <auto-generated />
// This file is auto-generated. Do not edit manually.
// Structure: HashTableCompact
#nullable enable
using System;
using System.Collections.Generic;
using System.Runtime.CompilerServices;
using System.Runtime.InteropServices;

internal static class FastData
{


    [StructLayout(LayoutKind.Auto)]
    private struct E
    {
        internal ushort Key;
        internal E(ushort key)
        {
            Key = key;
    }
    };

    private static readonly byte[] _bucketStarts = new byte[] {
        byte.MinValue, 1, 2, 3, 4
    };

    private static readonly E[] _entries = {
        new E(ushort.MinValue), new E(1), new E(2), new E(ushort.MaxValue)
    };

    [MethodImpl(MethodImplOptions.AggressiveInlining)]
    private static ulong Hash(ushort value)
    {
        unchecked
        {
        return (ulong)value;
        }
    }

    public static IEnumerable<ushort> Keys
    {
        get
        {
            for (int i = 0; i < _entries.Length; i++)
                yield return _entries[i].Key;
        }
    }

    
    public static bool Contains(ushort key)
    {


        ulong hash = Hash(key);
        int index = (int)(hash % 4);
        int start = (int)_bucketStarts[index];
        int end = (int)_bucketStarts[index + 1];

        for (int i = start; i < end; i++)
        {
            ref E entry = ref _entries[i];

            if (entry.Key == key)
                return true;
        }

        return false;
    }

    public const int ItemCount = 4;
    public const ushort MinKey = ushort.MinValue;
    public const ushort MaxKey = ushort.MaxValue;
}