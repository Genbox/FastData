// <auto-generated />
// This file is auto-generated. Do not edit manually.
// Structure: HashTable
#nullable enable
using System;
using System.Collections.Generic;
using System.Runtime.CompilerServices;
using System.Runtime.InteropServices;

internal static class FastData
{


    [StructLayout(LayoutKind.Auto)]
    private struct E
    {
        internal sbyte Key;
        internal sbyte Next;
        internal E(sbyte key, sbyte next)
        {
            Key = key;
            Next = next;
        }
    };

    private static readonly byte[] _buckets = new byte[] {
        3, 4, byte.MinValue, byte.MinValue, byte.MinValue, 2, byte.MinValue, 5, 1, byte.MinValue
    };

    private static readonly E[] _entries = {
        new E(sbyte.MinValue, -1), new E(-1, -1), new E(0, -1), new E(1, -1), new E(sbyte.MaxValue, -1)
    };

    [MethodImpl(MethodImplOptions.AggressiveInlining)]
    private static ulong Hash(sbyte value)
    {
        unchecked
        {
        return (ulong)value;
        }
    }

    public static IEnumerable<sbyte> Keys
    {
        get
        {
            for (int i = 0; i < _entries.Length; i++)
                yield return _entries[i].Key;
        }
    }

    
    public static bool Contains(sbyte key)
    {


        ulong hash = Hash(key);
        int index = (int)(hash % 10);
        int i = (int)_buckets[index] - 1;

        while (i >= 0)
        {
            ref E entry = ref _entries[i];

            if (entry.Key == key)
                return true;

            i = entry.Next;
        }

        return false;
    }

    public const int ItemCount = 5;
    public const sbyte MinKey = sbyte.MinValue;
    public const sbyte MaxKey = sbyte.MaxValue;
}