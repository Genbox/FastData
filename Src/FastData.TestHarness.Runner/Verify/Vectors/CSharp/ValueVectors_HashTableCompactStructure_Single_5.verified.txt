// <auto-generated />
// This file is auto-generated. Do not edit manually.
// Structure: Auto (HashTableCompact)
#nullable enable
using System;
using System.Runtime.CompilerServices;
using System.Runtime.InteropServices;

internal static class HashTableCompactStructure_Single_5
{
    [StructLayout(LayoutKind.Auto)]
    private struct E
    {
        internal float Key;
        internal ulong HashCode;
        
        internal E(float key, ulong hashCode)
        {
            Key = key;
            HashCode = hashCode;
            
        }
    };

    private static readonly byte[] _bucketStarts = new byte[] {
        0, 1, 2, 3, 3
     };

    private static readonly byte[] _bucketCounts = new byte[] {
        1, 1, 1, 0, 2
     };

    private static readonly E[] _entries = {
        new E(0f, 0), new E(1f, 1065353216), new E(float.MinValue, 4286578687), new E(-1f, 3212836864), new E(float.MaxValue, 2139095039)
    };


    [MethodImpl(MethodImplOptions.AggressiveInlining)]
    private static ulong Hash(float value)
    {
        uint bits = Unsafe.ReadUnaligned<uint>(ref Unsafe.As<float, byte>(ref value));

        if (((bits - 1) & ~(0x8000_0000)) >= 0x7FF0_0000)
            bits &= 0x7FF0_0000;

        return (ulong)bits;
    }

    
    public static bool Contains(float key)
    {
        if (key < float.MinValue || key > float.MaxValue)
            return false;


        ulong hash = Hash(key);
        uint index = (uint)(hash % 5);
        int start = (int)_bucketStarts[index];
        int count = (int)_bucketCounts[index];
        int end = start + count;

        for (int i = start; i < end; i++)
        {
            ref E entry = ref _entries[i];

            if (entry.HashCode == hash && entry.Key == key)
                return true;
        }

        return false;
    }

    public const uint ItemCount = 5;
    public const float MinKey = float.MinValue;
    public const float MaxKey = float.MaxValue;
}