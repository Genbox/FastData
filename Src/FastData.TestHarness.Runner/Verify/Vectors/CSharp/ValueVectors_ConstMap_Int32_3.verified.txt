// <auto-generated />
// This file is auto-generated. Do not edit manually.
// Structure: ConstMap
#nullable enable
using System;
using System.Collections.Generic;
using System.Runtime.CompilerServices;
using System.Runtime.InteropServices;

internal static class FastData
{



    private static readonly int[] _entries = {
        1, 2, 3
    };

    private static readonly byte[] _data = {
        byte.MinValue, byte.MinValue, byte.MinValue, 1, byte.MinValue, byte.MinValue, byte.MinValue, byte.MinValue, byte.MinValue, byte.MinValue, 
        byte.MinValue, byte.MinValue, byte.MinValue, byte.MinValue, byte.MinValue, byte.MinValue, 2, byte.MinValue, byte.MinValue, byte.MinValue, 
        byte.MinValue, 1, byte.MinValue, byte.MinValue
    };

    [MethodImpl(MethodImplOptions.AggressiveInlining)]
    private static ulong Murmur64(ulong hash)
    {
        unchecked
        {
            hash ^= hash >> 33;
            hash *= 0xff51afd7ed558ccd;
            hash ^= hash >> 33;
            hash *= 0xc4ceb9fe1a85ec53;
            hash ^= hash >> 33;
            return hash;
        }
    }

    [MethodImpl(MethodImplOptions.AggressiveInlining)]
    private static uint Reduce64(ulong hash, uint range)
    {
#if NET5_0_OR_GREATER
        return (uint)Math.BigMul(hash, range, out _);
#else
        unchecked
        {
            ulong lo = (uint)hash;
            ulong hi = hash >> 32;
            ulong loProduct = lo * range;
            ulong hiProduct = hi * range;
            ulong carry = (loProduct >> 32) + (uint)hiProduct;
            return (uint)((carry >> 32) + (hiProduct >> 32));
        }
#endif
    }

    [MethodImpl(MethodImplOptions.AggressiveInlining)]
    private static ulong Hash(int value)
    {
        unchecked
        {
        return (ulong)value;
        }
    }

    
    public static bool Contains(int key)
    {


        ulong hash = Murmur64(unchecked(Hash(key) + 10451216379200822465ul));
        uint mask = 7u;
        uint h0 = Reduce64(hash, 8u);
        uint h1 = (h0 + 8u) ^ (unchecked((uint)(hash >> 18)) & mask);
        uint h2 = (h0 + (2 * 8u)) ^ (unchecked((uint)hash) & mask);
        uint candidate = (uint)_data[h0] ^ (uint)_data[h1] ^ (uint)_data[h2];

        if (candidate >= 3u)
            return false;

        ref var entry = ref _entries[candidate];
        return key == entry;
    }

    public const int ItemCount = 3;
    public const int MinKey = 1;
    public const int MaxKey = 3;
}