// <auto-generated />
// This file is auto-generated. Do not edit manually.
// Structure: ConstMap
#nullable enable
using System;
using System.Collections.Generic;
using System.Runtime.CompilerServices;
using System.Runtime.InteropServices;

internal static class FastData
{

    private static int Length(string str) => str.Length;
    private static uint ReadU8(ReadOnlySpan<byte> ptr, int offset) => Unsafe.Add(ref MemoryMarshal.GetReference(ptr), offset);
    private static uint ReadU32(ReadOnlySpan<byte> ptr, int offset) => Unsafe.ReadUnaligned<uint>(ref Unsafe.Add(ref MemoryMarshal.GetReference(ptr), offset));
    private static ulong ReadU64(ReadOnlySpan<byte> ptr, int offset) => Unsafe.ReadUnaligned<ulong>(ref Unsafe.Add(ref MemoryMarshal.GetReference(ptr), offset));


    private static readonly string[] _entries = {
        "ß", "à", "ä", "å", "æ", "ç", "è", "é", "ê", "ñ", 
        "ö", "ø", "ü"
    };

    private static readonly byte[] _data = {
        2, byte.MinValue, byte.MinValue, 5, byte.MinValue, byte.MinValue, byte.MinValue, 11, 1, byte.MinValue, 
        byte.MinValue, byte.MinValue, byte.MinValue, byte.MinValue, byte.MinValue, byte.MinValue, byte.MinValue, byte.MinValue, byte.MinValue, byte.MinValue, 
        byte.MinValue, byte.MinValue, byte.MinValue, 8, byte.MinValue, byte.MinValue, byte.MinValue, 6, byte.MinValue, byte.MinValue, 
        2, byte.MinValue, 4, byte.MinValue, 8, 10, byte.MinValue, byte.MinValue, byte.MinValue, byte.MinValue, 
        byte.MinValue, byte.MinValue, byte.MinValue, 12, 5, byte.MinValue, byte.MinValue, byte.MinValue
    };

    [MethodImpl(MethodImplOptions.AggressiveInlining)]
    private static ulong Murmur64(ulong hash)
    {
        unchecked
        {
            hash ^= hash >> 33;
            hash *= 0xff51afd7ed558ccd;
            hash ^= hash >> 33;
            hash *= 0xc4ceb9fe1a85ec53;
            hash ^= hash >> 33;
            return hash;
        }
    }

    [MethodImpl(MethodImplOptions.AggressiveInlining)]
    private static uint Reduce64(ulong hash, uint range)
    {
#if NET5_0_OR_GREATER
        return (uint)Math.BigMul(hash, range, out _);
#else
        unchecked
        {
            ulong lo = (uint)hash;
            ulong hi = hash >> 32;
            ulong loProduct = lo * range;
            ulong hiProduct = hi * range;
            ulong carry = (loProduct >> 32) + (uint)hiProduct;
            return (uint)((carry >> 32) + (hiProduct >> 32));
        }
#endif
    }

    [MethodImpl(MethodImplOptions.AggressiveInlining)]
    private static ulong Hash(string value, int length)
    {
        unchecked
        {
        ReadOnlySpan<byte> data = MemoryMarshal.AsBytes(value.AsSpan());
        int offset = 0;
        int remaining = length;
        ulong hash;
        ulong round;
        ulong v1;
        ulong v2;
        ulong v3;
        ulong v4;
        if ((remaining >= 32))
        {
         v1 = 6983438078262162902ul;
         v2 = 14029467366897019727ul;
         v3 = ulong.MinValue;
         v4 = 7046029288634856825ul;
         while ((remaining >= 32))
         {
          v1 = unchecked((v1 + ((ulong)ReadU64(data, offset) * 14029467366897019727ul)));
          v1 = ((v1 << 31) | (v1 >> 33));
          v1 = unchecked((v1 * 11400714785074694791ul));
          v2 = unchecked((v2 + ((ulong)ReadU64(data, (offset + 8)) * 14029467366897019727ul)));
          v2 = ((v2 << 31) | (v2 >> 33));
          v2 = unchecked((v2 * 11400714785074694791ul));
          v3 = unchecked((v3 + ((ulong)ReadU64(data, (offset + 16)) * 14029467366897019727ul)));
          v3 = ((v3 << 31) | (v3 >> 33));
          v3 = unchecked((v3 * 11400714785074694791ul));
          v4 = unchecked((v4 + ((ulong)ReadU64(data, (offset + 24)) * 14029467366897019727ul)));
          v4 = ((v4 << 31) | (v4 >> 33));
          v4 = unchecked((v4 * 11400714785074694791ul));
          offset += 32;
          remaining -= 32;
         }

         hash = unchecked(((((v1 << 1) | (v1 >> 63)) + ((v2 << 7) | (v2 >> 57))) + (((v3 << 12) | (v3 >> 52)) + ((v4 << 18) | (v4 >> 46)))));
         round = unchecked((ulong.MinValue + (v1 * 14029467366897019727ul)));
         round = ((round << 31) | (round >> 33));
         round = unchecked((round * 11400714785074694791ul));
         hash = (hash ^ round);
         hash = unchecked(((hash * 11400714785074694791ul) + 9650029242287828579ul));
         round = unchecked((ulong.MinValue + (v2 * 14029467366897019727ul)));
         round = ((round << 31) | (round >> 33));
         round = unchecked((round * 11400714785074694791ul));
         hash = (hash ^ round);
         hash = unchecked(((hash * 11400714785074694791ul) + 9650029242287828579ul));
         round = unchecked((ulong.MinValue + (v3 * 14029467366897019727ul)));
         round = ((round << 31) | (round >> 33));
         round = unchecked((round * 11400714785074694791ul));
         hash = (hash ^ round);
         hash = unchecked(((hash * 11400714785074694791ul) + 9650029242287828579ul));
         round = unchecked((ulong.MinValue + (v4 * 14029467366897019727ul)));
         round = ((round << 31) | (round >> 33));
         round = unchecked((round * 11400714785074694791ul));
         hash = (hash ^ round);
         hash = unchecked(((hash * 11400714785074694791ul) + 9650029242287828579ul));
        }
        else
        {
         hash = 2870177450012600261ul;
        }

        hash += unchecked((ulong)length);
        while ((remaining >= 8))
        {
         round = unchecked((ulong.MinValue + ((ulong)ReadU64(data, offset) * 14029467366897019727ul)));
         round = ((round << 31) | (round >> 33));
         round = unchecked((round * 11400714785074694791ul));
         hash = (hash ^ round);
         hash = unchecked(((((hash << 27) | (hash >> 37)) * 11400714785074694791ul) + 9650029242287828579ul));
         offset += 8;
         remaining -= 8;
        }

        if ((remaining >= 4))
        {
         hash = (hash ^ unchecked(((ulong)ReadU32(data, offset) * 11400714785074694791ul)));
         hash = unchecked(((((hash << 23) | (hash >> 41)) * 14029467366897019727ul) + 1609587929392839161ul));
         offset += 4;
         remaining -= 4;
        }

        while ((remaining > 0))
        {
         hash = (hash ^ unchecked(((ulong)ReadU8(data, offset) * 2870177450012600261ul)));
         hash = unchecked((((hash << 11) | (hash >> 53)) * 11400714785074694791ul));
         offset += 1;
         remaining -= 1;
        }

        hash = (hash ^ (hash >> 33));
        hash = unchecked((hash * 14029467366897019727ul));
        hash = (hash ^ (hash >> 29));
        hash = unchecked((hash * 1609587929392839161ul));
        hash = (hash ^ (hash >> 32));

        return hash;
        }
    }

    
    public static bool Contains(string key)
    {
        int length = Length(key);


        ulong hash = Murmur64(unchecked(Hash(key, length * 2) + 10451216379200822465ul));
        uint mask = 15u;
        uint h0 = Reduce64(hash, 16u);
        uint h1 = (h0 + 16u) ^ (unchecked((uint)(hash >> 18)) & mask);
        uint h2 = (h0 + (2 * 16u)) ^ (unchecked((uint)hash) & mask);
        uint candidate = (uint)_data[h0] ^ (uint)_data[h1] ^ (uint)_data[h2];

        if (candidate >= 13u)
            return false;

        ref var entry = ref _entries[candidate];
        return StringComparer.Ordinal.Equals(key, entry);
    }

    public const int ItemCount = 13;
    public const int MinKeyLength = 1;
    public const int MaxKeyLength = 1;
    public const bool HasNumber = false;
    public const bool HasUppercase = false;
    public const bool HasLowercase = true;
    public const bool HasSymbol = false;
    public const bool HasWhitespace = false;
}