//! This file is auto-generated. Do not edit manually.
//! Structure: HashTable
#![allow(non_camel_case_types)]
#![allow(unused_parens)]
#![allow(missing_docs)]
#![allow(unused_imports)]
#![allow(unused_variables)]
#![allow(unused_unsafe)]
use std::ptr;


pub struct fastdata;

struct E {
    key: f64,
    hash_code: u64,
    next: i8,
}

impl fastdata {
    const BUCKETS: [u8; 5] = [
        2, 4, u8::MIN, 3, 1
    ];

    const ENTRIES: [E; 4] = [
        E { hash_code: 18442240474082181119, next: -1, key: f64::MIN }, E { hash_code: u64::MIN, next: -1, key: 0f64 }, E { hash_code: 4607182418800017408, next: -1, key: 1f64 }, E { hash_code: 9218868437227405311, next: -1, key: f64::MAX }
    ];

    #[inline(always)]
    fn get_hash(value: f64) -> u64 {
        let mut bits = value.to_bits();

        if ((bits.wrapping_sub(1)) & !0x8000_0000_0000_0000) >= 0x7FF0_0000_0000_0000 {
            bits &= 0x7FF0_0000_0000_0000;
        }
        bits
    }

    #[must_use]
    pub fn keys() -> impl Iterator<Item = f64> {
        Self::ENTRIES.iter().map(|entry| entry.key)
    }

    #[must_use]
    pub fn contains(key: f64) -> bool {

        let hash = Self::get_hash(key);
        let index = hash % 5u64;
        let mut i: i32 = (Self::BUCKETS[index as usize] as i32) - 1;

        while i >= 0 {
            let entry = &Self::ENTRIES[i as usize];
            if (entry.hash_code as u64) == hash && entry.key == key {
                return true;
            }
            i = entry.next as i32;
        }

        false
    }

    pub const ITEM_COUNT: usize = 4;
    pub const MIN_KEY: f64 = f64::MIN;
    pub const MAX_KEY: f64 = f64::MAX;
}