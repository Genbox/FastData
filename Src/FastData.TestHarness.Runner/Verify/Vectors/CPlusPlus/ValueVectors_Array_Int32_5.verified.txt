/// @generated
// This file is auto-generated. Do not edit manually.
// Structure: Array
#include <array>
#include <cstddef>
#include <cstring>
#include <cstdint>
#include <limits>
#include <string_view>


template<typename T>
class direct_access_view final {
    const T* _data;
    size_t _size;

public:
    constexpr direct_access_view(const T* data, const size_t size) noexcept : _data(data), _size(size) {}

    [[nodiscard]] constexpr const T* data() const noexcept { return _data; }
    [[nodiscard]] constexpr size_t size() const noexcept { return _size; }
    [[nodiscard]] constexpr const T& operator[](const size_t index) const noexcept { return _data[index]; }
    [[nodiscard]] constexpr const T* begin() const noexcept { return _data; }
    [[nodiscard]] constexpr const T* end() const noexcept { return _data + _size; }
};

class fastdata final {



    static constexpr std::array<int32_t, 5> key_items = {
        std::numeric_limits<int32_t>::lowest(), -1, 0, 1, std::numeric_limits<int32_t>::max()
    };

public:
    [[nodiscard]]
    static constexpr direct_access_view<int32_t> keys() noexcept { return direct_access_view<int32_t>(key_items.data(), 5); }
    [[nodiscard]]
    static constexpr bool contains(const int32_t key) noexcept {

        for (size_t i = 0; i < 5; i++) {
            if (key_items[i] == key)
                return true;
        }
        return false;
    }

    static constexpr size_t item_count = 5;
    static constexpr int32_t min_key = std::numeric_limits<int32_t>::lowest();
    static constexpr int32_t max_key = std::numeric_limits<int32_t>::max();
};