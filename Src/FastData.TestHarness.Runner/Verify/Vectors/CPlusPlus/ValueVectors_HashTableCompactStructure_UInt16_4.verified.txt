// This file is auto-generated. Do not edit manually.
// Structure: HashTableCompact
#pragma once
#include <array>
#include <cstring>
#include <cstdint>
#include <limits>
#include <string_view>

class HashTableCompactStructure_UInt16_4 final {
    struct e {
        uint16_t key;
        
        
        e(const uint16_t key)
           : key(key) {}
    };

    static constexpr std::array<uint8_t, 4> bucket_starts = {
        0, 1, 2, 3
     };

    static constexpr std::array<uint8_t, 4> bucket_counts = {
        1, 1, 1, 1
     };

    inline static const std::array<e, 4> entries = {
        e(0), e(1), e(2), e(std::numeric_limits<uint16_t>::max())
    };

    static constexpr uint64_t get_hash(const uint16_t value) noexcept
    {
        return static_cast<uint64_t>(value);
    }

public:
    [[nodiscard]]
    static constexpr bool contains(const uint16_t key) noexcept {
        if (key < 0 || key > std::numeric_limits<uint16_t>::max())
            return false;


        const uint64_t hash = get_hash(key);
        const size_t index = hash % 4;
        const size_t start = static_cast<size_t>(bucket_starts[index]);
        const size_t count = static_cast<size_t>(bucket_counts[index]);
        const size_t end = start + count;

        for (size_t i = start; i < end; i++) {
            const auto& entry = entries[i];

            if (entry.key == key)
                return true;
        }

        return false;
    }

    static constexpr size_t item_count = 4;
    static constexpr uint16_t min_key = 0;
    static constexpr uint16_t max_key = std::numeric_limits<uint16_t>::max();
};