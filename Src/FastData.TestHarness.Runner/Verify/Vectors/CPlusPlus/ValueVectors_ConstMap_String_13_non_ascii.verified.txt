/// @generated
// This file is auto-generated. Do not edit manually.
// Structure: ConstMap
#include <array>
#include <cstddef>
#include <cstring>
#include <cstdint>
#include <limits>
#include <string_view>


template<typename T>
class direct_access_view final {
    const T* _data;
    size_t _size;

public:
    constexpr direct_access_view(const T* data, const size_t size) noexcept : _data(data), _size(size) {}

    [[nodiscard]] constexpr const T* data() const noexcept { return _data; }
    [[nodiscard]] constexpr size_t size() const noexcept { return _size; }
    [[nodiscard]] constexpr const T& operator[](const size_t index) const noexcept { return _data[index]; }
    [[nodiscard]] constexpr const T* begin() const noexcept { return _data; }
    [[nodiscard]] constexpr const T* end() const noexcept { return _data + _size; }
};

class fastdata final {

    static constexpr int32_t Length(std::string_view str) noexcept { return static_cast<int32_t>(str.length()); }
    static uint32_t ReadU8(const std::string_view value, const size_t offset) noexcept { return static_cast<unsigned char>(value[offset]); }
    static uint32_t ReadU32(const std::string_view value, const size_t offset) noexcept { uint32_t result; std::memcpy(&result, value.data() + offset, sizeof(result)); return result; }
    static uint64_t ReadU64(const std::string_view value, const size_t offset) noexcept { uint64_t result; std::memcpy(&result, value.data() + offset, sizeof(result)); return result; }


    inline static const std::array<std::string_view, 13> entries = {
        u8"ß", u8"à", u8"ä", u8"å", u8"æ", u8"ç", u8"è", u8"é", u8"ê", u8"ñ", 
        u8"ö", u8"ø", u8"ü"
    };

    inline static const std::array<uint8_t, 48> data = {
        11, 0, 0, 14, 0, 0, 0, 2, 0, 0, 
        0, 0, 0, 0, 0, 0, 7, 0, 0, 0, 
        0, 0, 0, 0, 0, 0, 2, 0, 0, 0, 
        0, 0, 0, 0, 0, 11, 2, 15, 0, 6, 
        13, 0, 0, 0, 14, 1, 4, 0
    };

    static constexpr uint64_t murmur64(uint64_t hash) noexcept {
        hash ^= hash >> 33;
        hash *= 0xff51afd7ed558ccdULL;
        hash ^= hash >> 33;
        hash *= 0xc4ceb9fe1a85ec53ULL;
        hash ^= hash >> 33;
        return hash;
    }

    static constexpr uint64_t get_hash(const std::string_view value, int32_t length) noexcept {
        const auto data = value;
        int32_t offset = 0;
        int32_t remaining = length;
        uint64_t hash;
        uint64_t round;
        uint64_t v1;
        uint64_t v2;
        uint64_t v3;
        uint64_t v4;
        if ((remaining >= 32))
        {
         v1 = 6983438078262162902ull;
         v2 = 14029467366897019727ull;
         v3 = 0;
         v4 = 7046029288634856825ull;
         while ((remaining >= 32))
         {
          v1 = (v1 + (static_cast<uint64_t>(ReadU64(data, offset)) * 14029467366897019727ull));
          v1 = ((v1 << 31) | (v1 >> 33));
          v1 = (v1 * 11400714785074694791ull);
          v2 = (v2 + (static_cast<uint64_t>(ReadU64(data, (offset + 8))) * 14029467366897019727ull));
          v2 = ((v2 << 31) | (v2 >> 33));
          v2 = (v2 * 11400714785074694791ull);
          v3 = (v3 + (static_cast<uint64_t>(ReadU64(data, (offset + 16))) * 14029467366897019727ull));
          v3 = ((v3 << 31) | (v3 >> 33));
          v3 = (v3 * 11400714785074694791ull);
          v4 = (v4 + (static_cast<uint64_t>(ReadU64(data, (offset + 24))) * 14029467366897019727ull));
          v4 = ((v4 << 31) | (v4 >> 33));
          v4 = (v4 * 11400714785074694791ull);
          offset += 32;
          remaining -= 32;
         }

         hash = ((((v1 << 1) | (v1 >> 63)) + ((v2 << 7) | (v2 >> 57))) + (((v3 << 12) | (v3 >> 52)) + ((v4 << 18) | (v4 >> 46))));
         round = (0 + (v1 * 14029467366897019727ull));
         round = ((round << 31) | (round >> 33));
         round = (round * 11400714785074694791ull);
         hash = (hash ^ round);
         hash = ((hash * 11400714785074694791ull) + 9650029242287828579ull);
         round = (0 + (v2 * 14029467366897019727ull));
         round = ((round << 31) | (round >> 33));
         round = (round * 11400714785074694791ull);
         hash = (hash ^ round);
         hash = ((hash * 11400714785074694791ull) + 9650029242287828579ull);
         round = (0 + (v3 * 14029467366897019727ull));
         round = ((round << 31) | (round >> 33));
         round = (round * 11400714785074694791ull);
         hash = (hash ^ round);
         hash = ((hash * 11400714785074694791ull) + 9650029242287828579ull);
         round = (0 + (v4 * 14029467366897019727ull));
         round = ((round << 31) | (round >> 33));
         round = (round * 11400714785074694791ull);
         hash = (hash ^ round);
         hash = ((hash * 11400714785074694791ull) + 9650029242287828579ull);
        }
        else
        {
         hash = 2870177450012600261ull;
        }

        hash += static_cast<uint64_t>(length);
        while ((remaining >= 8))
        {
         round = (0 + (static_cast<uint64_t>(ReadU64(data, offset)) * 14029467366897019727ull));
         round = ((round << 31) | (round >> 33));
         round = (round * 11400714785074694791ull);
         hash = (hash ^ round);
         hash = ((((hash << 27) | (hash >> 37)) * 11400714785074694791ull) + 9650029242287828579ull);
         offset += 8;
         remaining -= 8;
        }

        if ((remaining >= 4))
        {
         hash = (hash ^ (static_cast<uint64_t>(ReadU32(data, offset)) * 11400714785074694791ull));
         hash = ((((hash << 23) | (hash >> 41)) * 14029467366897019727ull) + 1609587929392839161ull);
         offset += 4;
         remaining -= 4;
        }

        while ((remaining > 0))
        {
         hash = (hash ^ (static_cast<uint64_t>(ReadU8(data, offset)) * 2870177450012600261ull));
         hash = (((hash << 11) | (hash >> 53)) * 11400714785074694791ull);
         offset += 1;
         remaining -= 1;
        }

        hash = (hash ^ (hash >> 33));
        hash = (hash * 14029467366897019727ull);
        hash = (hash ^ (hash >> 29));
        hash = (hash * 1609587929392839161ull);
        hash = (hash ^ (hash >> 32));

        return hash;
    }

public:
    [[nodiscard]]
    static constexpr bool contains(const std::string_view key) noexcept {
        int32_t length = Length(key);

        const uint64_t hash = murmur64(get_hash(key, length) + 10451216379200822465ULL);
        const uint32_t h0 = static_cast<uint32_t>(static_cast<__uint128_t>(hash) * 16ULL >> 64);
        const uint32_t h1 = (h0 + 16U) ^ (static_cast<uint32_t>(hash >> 18) & 15U);
        const uint32_t h2 = (h0 + (2U * 16U)) ^ (static_cast<uint32_t>(hash) & 15U);
        const uint32_t candidate = static_cast<uint32_t>(data[h0]) ^ static_cast<uint32_t>(data[h1]) ^ static_cast<uint32_t>(data[h2]);

        if (candidate >= 13U)
            return false;

        const auto& entry = entries[candidate];
        return key == entry;
    }

    static constexpr size_t item_count = 13;
    static constexpr int32_t min_key_length = 2;
    static constexpr int32_t max_key_length = 2;
    static constexpr bool has_number = false;
    static constexpr bool has_uppercase = false;
    static constexpr bool has_lowercase = true;
    static constexpr bool has_symbol = false;
    static constexpr bool has_whitespace = false;
};