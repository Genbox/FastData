/// @generated
// This file is auto-generated. Do not edit manually.
// Structure: Pgm
#include <array>
#include <cstddef>
#include <cstring>
#include <cstdint>
#include <limits>
#include <string_view>


template<typename T>
class direct_access_view final {
    const T* _data;
    size_t _size;

public:
    constexpr direct_access_view(const T* data, const size_t size) noexcept : _data(data), _size(size) {}

    [[nodiscard]] constexpr const T* data() const noexcept { return _data; }
    [[nodiscard]] constexpr size_t size() const noexcept { return _size; }
    [[nodiscard]] constexpr const T& operator[](const size_t index) const noexcept { return _data[index]; }
    [[nodiscard]] constexpr const T* begin() const noexcept { return _data; }
    [[nodiscard]] constexpr const T* end() const noexcept { return _data + _size; }
};

class fastdata final {


    static constexpr std::array<float, 5> key_items = {
        1.0f, 2.0f, 3.0f, 4.0f, 5.0f
    };

public:
    [[nodiscard]]
    static constexpr direct_access_view<float> keys() noexcept { return direct_access_view<float>(key_items.data(), 5); }
    [[nodiscard]]
    static constexpr bool contains(const float key) noexcept {

        const double product = static_cast<double>(0.50000095f) * static_cast<double>(key - 1.0f);
        int32_t pos = product <= 0.0 ? 0 : (product >= static_cast<double>(0) ? 0 : static_cast<int32_t>(product));
        pos += 31;
        if (pos > 5) pos = 5;

        size_t lo = pos >= 64 ? static_cast<size_t>(pos - 64) : 0;
        size_t hi = static_cast<size_t>(pos) + 66;
        if (hi > 5) hi = 5;

        while (lo < hi) {
            const size_t mid = lo + ((hi - lo) >> 1);
            const float mid_key = key_items[mid];
            if (mid_key == key)
                return true;
            if (mid_key < key)
                lo = mid + 1;
            else
                hi = mid;
        }

        return false;
    }

    static constexpr size_t item_count = 5;
    static constexpr float min_key = 1.0f;
    static constexpr float max_key = 5.0f;
};