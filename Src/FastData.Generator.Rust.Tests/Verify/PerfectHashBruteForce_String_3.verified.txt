//! This file is auto-generated. Do not edit manually.
//! Structure: PerfectHashBruteForce
#![allow(unused_parens)]
#![allow(missing_docs)]
pub struct MyData;

impl MyData {
    const ENTRIES: [E; 3] = [
        E { value: "a", hash_code: 2150936925 }, E { value: "aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa", hash_code: 119911210 }, E { value: "item", hash_code: 1699185842 }
    ];

    fn get_hash(value: &str, seed: u32) -> u32 {
        let mut hash1: u32 = seed;
        let mut hash2: u32 = seed;

        for chunk in value.as_bytes().chunks(8) {
            if chunk.len() >= 4 {
                let part1 = u32::from_le_bytes(chunk[0..4].try_into().unwrap());
                hash1 = hash1.rotate_left(5) ^ part1;
            }
            if chunk.len() == 8 {
                let part2 = u32::from_le_bytes(chunk[4..8].try_into().unwrap());
                hash2 = hash2.rotate_left(5) ^ part2;
            }
        }

        for &b in value.as_bytes().iter().skip(value.len() / 8 * 8) {
            hash2 = hash2.rotate_left(5) ^ b as u32;
        }

        hash1.wrapping_add(hash2.wrapping_mul(0x5D588B65))
    }

    pub fn contains(value: &str) -> bool {
        if value.len() < 1 || value.len() > 255 {
            return false;
        }

        let hash = Self::get_hash(value, 4);
        let index = (hash % 3) as usize;
        let entry = &Self::ENTRIES[index];

        hash == entry.hash_code && value == entry.value
    }

    pub const ITEM_COUNT: usize = 3;
    pub const MIN_LENGTH: usize = 1;
    pub const MAX_LENGTH: usize = 255;
}
struct E {
    value: &'static str,
    hash_code: u32,
}
