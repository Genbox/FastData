//! This file is auto-generated. Do not edit manually.
//! Structure: HashSetChain
#![allow(unused_parens)]
#![allow(missing_docs)]
pub struct MyData;

impl MyData {
    const BUCKETS: [i8; 3] = [
        3, 2, 0
    ];

    const ENTRIES: [E; 3] = [
        E { hash_code: 0, next: -1, value: 0 }, E { hash_code: 1, next: -1, value: 1 }, E { hash_code: 255, next: 0, value: 255 }
    ];

    fn get_hash(value: u8) -> u32 {
        (value as u32)
    }

    pub fn contains(value: u8) -> bool {
        if value < 0 || value > 255 {
            return false;
        }

        let hash = Self::get_hash(value);
        let index = hash % 3;
        let mut i: i8 = (Self::BUCKETS[index as usize] as i8) - 1;

        while i >= 0 {
            let entry = &Self::ENTRIES[i as usize];
            if entry.hash_code == hash && value == entry.value {
                return true;
            }
            i = entry.next;
        }

        false
    }

    pub const ITEM_COUNT: usize = 3;
    pub const MIN_VALUE: u8 = 0;
    pub const MAX_VALUE: u8 = 255;
}
struct E {
    hash_code: u32,
    next: i8,
    value: u8,
}
