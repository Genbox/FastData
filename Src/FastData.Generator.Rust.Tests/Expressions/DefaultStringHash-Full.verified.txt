        let mut i32 length;
        let mut i32 offset;
        let mut u64 hash;
        offset = 0;
        hash = 352654597;
        length = value.len();
        while ((length > 0))
        {
            hash = hash = ((((hash << 5) | (hash >> 27)) + hash) ^ ReadU8(value, offset));
            offset += 1;
            length -= 1;
        }

        hash = (352654597 + (hash * 1566083941));
