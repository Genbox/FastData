// <auto-generated />
// This file is auto-generated. Do not edit manually.
// Structure: Auto (HashTableCompact)
#nullable enable
using System;
using System.Runtime.CompilerServices;
using System.Runtime.InteropServices;

internal static class HashTableCompactStructure_SByte_5
{
    [StructLayout(LayoutKind.Auto)]
    private struct E
    {
        internal sbyte Key;
        
        
        internal E(sbyte key)
        {
            Key = key;
            
            
        }
    };

    private static readonly byte[] _bucketStarts = new byte[] {
        0, 2, 3, 4, 5
     };

    private static readonly byte[] _bucketCounts = new byte[] {
        2, 1, 1, 1, 0
     };

    private static readonly E[] _entries = {
        new E(-1), new E(0), new E(1), new E(sbyte.MaxValue), new E(sbyte.MinValue)
    };


    [MethodImpl(MethodImplOptions.AggressiveInlining)]
    private static ulong Hash(sbyte value)
    {
        return (ulong)value;
    }

    
    public static bool Contains(sbyte key)
    {
        if (key < sbyte.MinValue || key > sbyte.MaxValue)
            return false;

        ulong hash = Hash(key);
        uint index = (uint)(hash % 5);
        int start = (int)_bucketStarts[index];
        int count = (int)_bucketCounts[index];
        int end = start + count;

        for (int i = start; i < end; i++)
        {
            ref E entry = ref _entries[i];

            if (entry.Key == key)
                return true;
        }

        return false;
    }

    public const uint ItemCount = 5;
    public const sbyte MinKey = sbyte.MinValue;
    public const sbyte MaxKey = sbyte.MaxValue;
}