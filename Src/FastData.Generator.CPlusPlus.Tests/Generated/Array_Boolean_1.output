#include <string>
#include <array>
#include <cstdint>

class Array_Boolean_1
{
    inline static const std::array<bool, 1> entries = {
        true
    };

public:
    static bool contains(const bool& value)
    {


        for (int i = 0; i < 1; i++)
        {
            if (value == entries[i])
               return true;
        }
        return false;
    }

    static constexpr int item_count = 1;
};