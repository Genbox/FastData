#include <string>
#include <array>
#include <cstdint>

class Conditional_UInt32_1
{
public:
    static bool contains(const uint32_t& value)
    {
       if (const size_t len = value.length(); len != 1)
           return false;

        if (value == 1)
            return true;

        return false;
    }

    static constexpr int item_count = 1;
};