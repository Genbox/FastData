// This file is auto-generated. Do not edit manually.
// Structure: HashSetChain
#pragma once
#include <array>
#include <cstdint>
#include <limits>

class my_data
{
    struct e
    {
        uint32_t hash_code;
        int8_t next;
        int64_t value;

        e(const uint32_t hash_code, const int8_t next, const int64_t value)
           : hash_code(hash_code), next(next), value(value) {}
    };

    static constexpr std::array<int8_t, 3> buckets = {
        2, 0, 3
     };

    inline static const std::array<e, 3> entries = {
        e(2147483648, -1, std::numeric_limits<int64_t>::lowest()), e(0, -1, 0ll), e(2147483648, 0, std::numeric_limits<int64_t>::max())
    };

    static uint32_t get_hash(const int64_t value) noexcept
    {
        return static_cast<uint32_t>(static_cast<int32_t>(value) ^ static_cast<int32_t>(value >> 32));
    }

public:
    static bool contains(const int64_t value) noexcept
    {
        if (value < std::numeric_limits<int64_t>::lowest() || value > std::numeric_limits<int64_t>::max())
            return false;

        const uint32_t hash = get_hash(value);
        const uint32_t index = hash % 3;
        int8_t i = static_cast<int8_t>(buckets[index] - 1);

        while (i >= 0)
        {
            const auto& [hash_code, next, value1] = entries[i];

            if (hash_code == hash && value1 == value)
                return true;

            i = next;
        }

        return false;
    }

    static constexpr unsigned int item_count = 3;
    static constexpr int64_t min_value = std::numeric_limits<int64_t>::lowest();
    static constexpr int64_t max_value = std::numeric_limits<int64_t>::max();
};