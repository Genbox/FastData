namespace Genbox.FastData.Generators;

/// <summary>Represents numeric constants used by code generators in the FastData library.</summary>
public sealed record NumericConstants(object MinValue, object MaxValue);